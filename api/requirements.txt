# ===========================================================================
# AI Sentinel — Complete Python Dependencies
# All open-source libraries needed for the full 6-layer scanning pipeline
# ===========================================================================

# ---------------------------------------------------------------------------
# Core API Server
# ---------------------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18
pydantic>=2.0

# ---------------------------------------------------------------------------
# AI / LLM Integration
# ---------------------------------------------------------------------------
openai==2.29.0

# ---------------------------------------------------------------------------
# Azure Cloud Integration
# ---------------------------------------------------------------------------
azure-identity==1.19.0
azure-storage-blob==12.24.0

# ---------------------------------------------------------------------------
# PDF Report Generation
# ---------------------------------------------------------------------------
fpdf2>=2.8.0

# ---------------------------------------------------------------------------
# Layer 1 — Code SAST
# ---------------------------------------------------------------------------
semgrep>=1.90.0

# ---------------------------------------------------------------------------
# Layer 1b — Agent Architecture Scanning
# ---------------------------------------------------------------------------
agentic-radar>=0.1.0
mcp-scan>=0.1.0

# ---------------------------------------------------------------------------
# Layer 2 — Model Artifact Scanning
# ---------------------------------------------------------------------------
modelscan>=0.8.0
picklescan>=0.0.16
fickling[torch]>=0.1.0
modelaudit[all]>=0.1.0
veritensor[all]>=0.1.0

# ---------------------------------------------------------------------------
# Layer 2 — ML Framework Libraries (for model format access & classes)
# ---------------------------------------------------------------------------
torch>=2.0
numpy>=1.26
h5py>=3.10
onnx>=1.16
safetensors>=0.4
tflite-runtime>=2.14; platform_machine != "aarch64"
joblib>=1.3
dill>=0.3
cloudpickle>=3.0

# ---------------------------------------------------------------------------
# Layer 4 — Dependency Vulnerability Scanning
# ---------------------------------------------------------------------------
cve-bin-tool>=3.3

# ---------------------------------------------------------------------------
# Layer 5 — SBOM Generation & Governance
# ---------------------------------------------------------------------------
aisbom-cli>=0.1.0
cyclonedx-py>=4.0

# ---------------------------------------------------------------------------
# Build System (pyproject.toml requirement)
# ---------------------------------------------------------------------------
setuptools>=68.0

# ---------------------------------------------------------------------------
# Development & Testing
# ---------------------------------------------------------------------------
pytest>=8.0
ruff>=0.5.0
